# 安全策略

## 支持范围

安全修复以 `main` 分支的最新版本为准。旧提交、历史分支、未合并分支和第三方 fork 不保证获得安全修复。

## 系统与安全范围

ms079 是基于 Java 8 的 MapleStory v079 服务端。安全评估重点包括：

- 登录、频道、商城等对外网络入口及其数据包解析；
- 账号、角色、背包、交易、经济系统和 GM 权限边界；
- MySQL 数据访问、迁移和持久化完整性；
- JavaScript 脚本、WZ 数据及运行时配置的加载边界；
- Docker 镜像、Maven 与 GitHub Actions 依赖供应链。

## 威胁模型与信任边界

需要保护的资产包括账号凭据、角色和道具数据、游戏经济、GM/管理员权限、数据库完整性、服务可用性以及部署密钥。

默认视为不可信的输入包括匿名或已登录客户端发送的数据包、可由账号控制的数据、外部脚本或 WZ 数据、Pull Request 内容以及第三方依赖。管理员、数据库和运行时密钥仅在被明确配置的范围内视为可信；误提交、误暴露或越权使用不属于可信行为。

## 安全不变量

- 未完成身份验证的连接不得读取或修改账号、角色及游戏经济数据。
- 每次角色、道具、交易和管理操作都必须验证当前会话的所有权与权限，不能仅信任客户端标识。
- 网络数据包、脚本参数和持久化数据必须经过长度、范围、状态和资源上限校验。
- 数据库访问不得通过未转义输入拼接可执行 SQL；敏感操作应保持原子性，避免复制物品、重复结算或部分写入。
- 密码、令牌、连接凭据和会话秘密不得写入仓库、日志、构建产物或 Action 输出。
- 合入 `main` 的变更必须经过 Pull Request 和仓库规定的自动检查；工作流使用最小权限并固定 Action 完整提交 SHA。

## 可报告的问题与严重程度

欢迎报告具有现实攻击路径的问题，包括但不限于远程代码执行、身份验证绕过、越权访问、GM 权限提升、SQL 注入、敏感信息泄露、可重复利用的数据或经济破坏、持久化数据损坏，以及可由不可信输入稳定触发的拒绝服务。

- 严重：无需可信运维权限即可远程执行代码、大范围接管账号或获取部署/数据库密钥。
- 高危：身份验证绕过、GM/管理员提权、可利用 SQL 注入、跨账号资产读写或稳定的数据复制/破坏。
- 中低危：影响范围受限、需要较强前置条件，或仅造成有限信息泄露、完整性或可用性影响的问题。

最终严重程度会结合可达性、所需权限、影响范围、可重复性和现有缓解措施判断。

## 范围外与验证要求

以下情况通常不作为安全漏洞处理，除非能够证明跨越了服务端安全边界：

- 纯游戏平衡问题，或仅修改客户端即可实现但服务端未受影响的表现；
- 只列出过期组件或 CVE、但没有证明问题代码可达且对本项目有实际影响的报告；
- 需要先获得受信任主机管理员、数据库管理员或仓库管理员权限的同权限操作；
- 依赖不现实流量、且无法稳定复现的资源消耗；
- 社会工程、物理攻击以及对非本项目基础设施的测试。

请勿对真实玩家、第三方服务器或不属于你的数据进行测试。验证应使用自有隔离环境，并提供最小、无破坏性的复现方法。

## 已知限制与补偿措施

本项目包含 Java 8、MySQL 5.7、旧版游戏协议和动态脚本等遗留技术，这些技术会扩大维护和攻击面，但其存在本身不等于已确认漏洞。仓库通过 PR 必选测试、Docker 构建、依赖审查、Dependabot、Secret Scanning、推送保护和 CodeQL 降低风险；这些自动化不能替代对真实可达性和业务权限边界的人工验证。

## 私密报告流程

请使用 GitHub 的[私密漏洞报告](https://github.com/W1tFzQq08pTv/ms079/security/advisories/new)，不要先创建公开 Issue、讨论或 Pull Request。

报告请尽量包含：

- 受影响的提交、版本、入口和必要配置；
- 攻击前置条件、最小复现步骤或概念验证；
- 实际与预期行为、影响范围和严重程度判断；
- 建议修复方向，以及报告者希望获得的致谢信息。

维护者会尽力确认报告、协调修复和公开时间；在修复发布前，请避免公开可直接利用的细节。
